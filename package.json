{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.14.1"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start --stream",
    "tsc": "lerna run tsc --stream",
    "publish": "lerna run tsc && lerna publish",
    "watch": "lerna run watch --stream"
  },
  "dependencies": {
    "@lerna-new-app-creation-react-module/module1": "0.0.3",
    "@types/antd": "^1.0.0",
    "@types/node": "^12.0.7",
    "axios": "^0.21.1",
    "save-dev": "^2.0.0",
    "typescript": "^3.5.1"
  }
}
